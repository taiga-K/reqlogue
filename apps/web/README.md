# @reqlogue/web

Next.js App Router + Feature-Sliced Design. Next.js folders live at `app/`. FSD layers live in `src/` as `_app` and `_pages` to avoid name clashes.

```bash
pnpm --filter @reqlogue/web dev
pnpm --filter @reqlogue/web verify
```
